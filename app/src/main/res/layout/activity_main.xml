<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:divider="#000000"
    android:listSelector="@android:color/holo_green_light"
    android:orientation="vertical"
    tools:context="com.example.android.booklistinggoogle.MainActivity">

    <TextView
        android:id="@+id/book_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="enter book title or author below:"
        android:textAllCaps="true"
        android:textSize="16sp"
        android:textStyle="bold" />

    <EditText
        android:id="@+id/book_edit_text"
        android:layout_width="260dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/book_textView"
        android:layout_centerHorizontal="true"
        android:hint="Title or Author"
        android:inputType="textCapWords" />

    <Button
        android:id="@+id/search_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/book_edit_text"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text="ready to read" />

</RelativeLayout>
